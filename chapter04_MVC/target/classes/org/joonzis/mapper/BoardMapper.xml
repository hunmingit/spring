<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.joonzis.mapper.BoardMapper">
	<!-- getList() 메소드 이름이 id 임  -->
	<select id="getList" resultType="org.joonzis.domain.BoardVO">
	<!-- <, > 들어가면 CDATA 사용 -->
		<![CDATA[ 
		SELECT bno, writer, title, regdate, replycnt
		from (Select ROWNUM rn, a.* 
			from
			(Select bno, writer, title, regdate, replycnt 
			from tbl_board order by bno desc) a
			where ROWNUM <= #{pageNum} * #{amount})
			where rn > (#{pageNum}-1) * #{amount}
		]]>
	</select>
	<!-- 전체 게시글 수 가져오기 getTotal -->
	<select id="getTotal" resultType="int">
		SELECT COUNT(*) FROM tbl_board
	</select>
	
	
	<insert id="insert" parameterType="org.joonzis.domain.BoardVO"> <!-- regdate, updatedate default값 sysdate로 정해져있어서 안넣어도됨 -->
<!-- selectKey를 사용한다면 insert문에서는 시퀀스를 절대 다시 호출하면 안 된다. -->	
<!-- insert 실행 전에 PK를 내가 미리 만들어서 VO에 넣어두겠다. --> 
<!-- DUAL을 이용해서 시퀀스(seq_board)의 다음 값(NEXTVAL)을 조회하고 그 값을 BoardVO.bno에 담아서 insert에 사용한다.--> 
	  <selectKey keyProperty="bno" order="BEFORE" resultType="int">
			    SELECT seq_board.NEXTVAL FROM dual
	  </selectKey>		
				insert into tbl_board (bno, title, content, writer, regdate)
				values(#{bno}, #{title}, #{content}, #{writer}, sysdate)
	</insert>	
	
	<select id="read"  parameterType="int" resultType="org.joonzis.domain.BoardVO">
		select * FROM tbl_board where bno = #{bno}
	</select>
	
	<delete id="delete" parameterType="int">
		delete FROM tbl_board where bno = #{bno}
	</delete>
	
	<update id="update" parameterType="org.joonzis.domain.BoardVO">
		update tbl_board 
		set title = #{title}, content= #{content}, writer= #{writer}, updatedate = sysdate
		where bno = #{bno}
	</update>
	<!-- 댓글 수 증가, 감소 -->
	<update id="updateReplyCnt">
		update tbl_board
		set replycnt = replycnt + #{amount}
		where bno = #{bno}	
	</update>

	
	
</mapper>
